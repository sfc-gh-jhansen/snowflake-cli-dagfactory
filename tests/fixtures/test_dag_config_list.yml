test_pipeline:
  schedule: "60 MINUTES"
  warehouse: "DEMO_WH"
  description: "Integration test pipeline (list format)"
  tasks:
    - task_id: step_one
      sql: "SELECT 1"
    - task_id: step_two
      sql: "SELECT 2"
      dependencies: [step_one]
    - task_id: step_three
      sql: "SELECT 3"
      dependencies: [step_one]
    - task_id: step_four
      sql: "SELECT 4"
      dependencies: [step_two, step_three]
